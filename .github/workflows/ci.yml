name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  actionlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Check workflow and action files
        uses: docker://rhysd/actionlint:latest
        with:
          args: -color

  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: ./actions/setup-jsonnet
      - uses: ./actions/setup-kubeconform
      - uses: ./actions/setup-vendir
